[package]
name = "revol"
version = "0.7.2"
edition = "2021"
authors = ["revol contributors", "Claude"]
description = """A modern package manager and build system for C and C++, with strict
project-layout enforcement and deep Clang integration."""
license = "MIT"
repository = "https://github.com/xntas/revol"

[[bin]]
name = "revol"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"