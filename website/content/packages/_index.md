---
title: "Packages"
lead: "The revoq registry — pull any of these in by shorthand in revoq.toml."
---

revoq's registry is a single flat-text file,
[`revoq-libs`](/revoq-libs), that maps a shorthand to a repository URL. `gh:user/repo`
shorthands resolve to GitHub on their own, so the index only carries entries whose
URL can't be guessed from the name. The table below is that file, rendered.
