baseURL: "https://revoq-cli.com/"
locale: "en-us"
title: "revoq"
theme: "revoq"

markup:
  goldmark:
    renderer:
      unsafe: true
  highlight:
    style: "base16-snazzy"
    noClasses: false

menu:
  main:
    - name: "Docs"
      url: "/docs/"
      weight: 1

params:
  version: "v0.7.2"
  description: "Cargo-style build system for C and C++ projects featuring deterministic lockfiles and parallel compilation."
  tagline: "Cargo-style build toolchain for C/C++"
