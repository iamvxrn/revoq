# deft Website

Static site for deft, built with Hugo. Origin: `https://deft-cli.com/`.

> Part of the [deft](https://github.com/xntas/deft) monorepo (`website/`). deft
> is an experiment in what an AI can build for C/C++ tooling — "Cargo, but for C
> and C++" — not a production tool.
