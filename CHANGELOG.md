# Changelog

revol's release notes live with the CLI: see [`cli/CHANGELOG.md`](cli/CHANGELOG.md).

This monorepo consolidates the former `revol-cli/{revol,website,example-app,json}`
repositories (history preserved) as of the 0.6.0 release.
