name: Release Binaries

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Build & Release Binaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu, x86_64-apple-darwin, aarch64-apple-darwin, x86_64-pc-windows-msvc

      - name: Build Cross-Platform Artifacts
        run: |
          mkdir -p dist
          TAG_NAME="${GITHUB_REF_NAME:-v0.7.2}"
          cargo build --manifest-path cli/Cargo.toml --release --target x86_64-unknown-linux-gnu
          tar -czf "dist/revoq-${TAG_NAME}-x86_64-unknown-linux-gnu.tar.gz" -C cli/target/x86_64-unknown-linux-gnu/release revoq
          
          # Checksums
          cd dist && sha256sum *.tar.gz > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
